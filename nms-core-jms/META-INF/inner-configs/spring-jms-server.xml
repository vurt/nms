<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:jms="http://www.springframework.org/schema/jms" xmlns:osgi="http://www.springframework.org/schema/osgi"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
            http://www.springframework.org/schema/context
            http://www.springframework.org/schema/context/spring-context-2.5.xsd
            http://www.springframework.org/schema/osgi
			http://www.springframework.org/schema/osgi/spring-osgi.xsd
            http://www.springframework.org/schema/jms 
            http://www.springframework.org/schema/jms/spring-jms-2.5.xsd">
	
    <!-- 负责发送广播的jmsTemplate -->
	<bean id="nmsTopicJmsTemplate" class="org.springframework.jms.core.JmsTemplate">
		<property name="connectionFactory" ref="jmsFactory" />
		<property name="defaultDestination" ref="nmsTopic" />
	</bean>
     
    <!-- 广播消息生产者 -->
	<bean id="topicProducer" class="me.vurt.nms.core.jms.impl.MessageProducerImpl">
		<property name="jmsTemplate" ref="nmsTopicJmsTemplate" />
	</bean>
	
	<!-- 注册广播消息生产者 -->
	<osgi:service  ref="topicProducer" interface="me.vurt.nms.core.jms.MessageProducer" />

	<!-- 心跳监听器 -->
	<bean id="heartBeatListener" class="me.vurt.nms.core.jms.impl.MessageListenerAdapter2">
		<constructor-arg ref="messageListener" />
		<property name="defaultListenerMethod" value="messageReceived" />
		<!-- 心跳不需要响应 -->
	</bean>
	
	<!-- 心跳监听容器 -->
	<bean id="heartBeatListenerContainer"
		class="org.springframework.jms.listener.DefaultMessageListenerContainer">
		<property name="connectionFactory" ref="connectionFactory" />
		<property name="destination" ref="heartBeatQueue" />
		<property name="messageListener" ref="heartBeatListener" />
	</bean>
	
	<!-- 注册监听器 -->
	<bean id="registrationListener" class="me.vurt.nms.core.jms.impl.MessageListenerAdapter2">
		<constructor-arg ref="messageListener" />
		<property name="defaultListenerMethod" value="messageReceived" />
		<property name="defaultResponseDestination" ref="registrationResponseQueue" />
	</bean>
	
	<!-- 注册监听容器 -->
	<bean id="registrationListenerContainer"
		class="org.springframework.jms.listener.DefaultMessageListenerContainer">
		<property name="connectionFactory" ref="connectionFactory" />
		<property name="destination" ref="registrationQueue" />
		<property name="messageListener" ref="registrationListener" />
	</bean>
</beans>